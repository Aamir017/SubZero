name: CI Pipeline for SubZero

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v2

    - name: 🐍 Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.10'

    - name: 📦 Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: 🧪 Run test: Start app and test response
      run: |
        nohup python app.py > log.txt 2>&1 &
        sleep 5
        curl -I http://127.0.0.1:5000 || echo "❌ App did not respond"

